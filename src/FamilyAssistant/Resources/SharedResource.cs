namespace FamilyAssistant;

/// <summary>
/// Marker class for the shared localization resource.
/// Must be in root namespace so the localizer finds Resources/SharedResource.{culture}.resx
/// (ResourcesPath="Resources" + namespace minus root = file path).
/// </summary>
public class SharedResource { }
